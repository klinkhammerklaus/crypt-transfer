#!/usr/bin/env bash
set -euo pipefa echo "Key file not found: $KEY_FILE"
  exit 1
fi

if [[ ! -s "$KEY_FILE" ]]; then
  echo "Key file is empty: $KEY_FILE"
  exit 1
fi

PASS="$(cat "$KEY_FILE")"

shopt -s nullglob
found=0

for f in "$IN_DIR"/*; do
  [[ -f "$f" ]] || continue
  found=1

  name="$(basename "$f")"
  out="$OUT_DIR/${name}.enc"

  # AES-256-CBC + PBKDF2 (strong key derivation)
  openssl enc -aes-256-cbc -salt -pbkdf2 -iter 200000     -in "$f" -out "$out" -pass "pass:$PASS"

  echo "✅ $name -> $(basename "$out")"
done

if [[ "$found" -eq 0 ]]; then
  echo "No files found in $IN_DIR"
fi
